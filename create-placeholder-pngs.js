#!/usr/bin/env node

/**
 * Create minimal placeholder PNG icons using base64-encoded data
 * These are tiny 1x1 black pixels that will be scaled up
 * Good enough for PWA installation, replace with real icons later
 */

const fs = require('fs');
const path = require('path');

// Minimal 1x1 black PNG (base64)
// This is a valid PNG file that browsers will scale up
const blackPixelPNG = Buffer.from(
  'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==',
  'base64'
);

// Better placeholder: actual sized black square with minimal data
// This creates a larger PNG but still very small filesize
const createMinimalPNG = (size) => {
  // For simplicity, we'll use the 1x1 black pixel
  // Browsers will scale it to the requested size
  return blackPixelPNG;
};

const iconsDir = path.join(__dirname, 'public', 'icons');

// Ensure directory exists
if (!fs.existsSync(iconsDir)) {
  fs.mkdirSync(iconsDir, { recursive: true });
}

const sizes = [192, 512];

sizes.forEach(size => {
  const pngPath = path.join(iconsDir, `icon-${size}.png`);
  fs.writeFileSync(pngPath, createMinimalPNG(size));
  console.log(`✅ Created minimal placeholder: ${pngPath}`);
});

console.log('\n⚠️  WARNING: These are minimal 1x1 black placeholders!');
console.log('   They will install the PWA but look ugly.');
console.log('   Replace with proper icons ASAP using one of these methods:');
console.log('\n   1. Use the SVG icons generated by generate-icons.js');
console.log('   2. Create proper PNG icons with a design tool');
console.log('   3. Use: npx pwa-asset-generator logo.svg public/icons');
console.log('\n✅ PWA will now install successfully!\n');
